# ТЗ: приложение для обучения чтению на арабском

Статус: черновик по итогам обсуждения. Всё, что помечено `?`, ещё не решено.

---

## 1. Продукт

Приложение учит человека **читать по-арабски с нуля**: от узнавания отдельных букв до чтения слов.
Не разговорник, не словарь, не переводчик.

- Язык интерфейса: русский первым, английский вторым. Локализация закладывается в архитектуру сразу.
- Платформы: iOS, Android (Flutter).
- Целевая аудитория: русскоязычные новички, в том числе те, кто уже видел буквы, но не читает.
- Два трека: **«Чтение»** (от букв до слов) и **«Таджвид»** (правила чтения Корана). Второй доступен как продолжение первого либо сразу — по входному тесту, см. §8.

---

## 2. Ключевая идея: атом знания

Единица обучения — **не буква**, а **атом**.

```
Atom = (буква × форма) | огласовка | правило | слог
```

Всё — прогресс, очередь повторений, разблокировки — считается по атомам.
Буква считается освоенной, когда освоены все её атомы.

### Формы

Модель честная: у разных букв разное число форм.

| Тип буквы | Формы | Кол-во |
|---|---|---|
| Соединяющиеся с обеих сторон | изолированная, начальная, средняя, конечная | 22 буквы × 4 |
| Не соединяющиеся влево (`ا د ذ ر ز و`) | изолированная, конечная | 6 букв × 2 |

Итого ≈ **100 атомов-форм**.

Правило «не соединяется со следующей буквой» — не отдельный урок, а **факт, показываемый в момент знакомства с буквой**, с примером разрыва в слове. Это правило проверяемо: в упражнении на сборку слова неверное соединение засчитывается как ошибка.

### Формы вводятся блоками внутри одного урока

После знакомства с отдельной буквой доступны её соединённые формы:

```
ب-изолированная  requires: понятие «точки»
ب-конечная       requires: ب-изолированная показана
ب-начальная      requires: ب-изолированная показана
ب-средняя        requires: ب-изолированная показана
```

Порядок не случаен: конечная форма ближе всего к изолированной (та же буква плюс соединительная черта справа), поэтому идёт второй. Начальная и средняя отличаются сильнее.

**Буква в `known`** = все её формы в `known`. Именно это считают пороговые условия тем.

Следствие по длине: до порога «8 букв» нужно 32 атома в `known`. При вводе 1–2 атомов за урок это ~20–25 уроков до огласовок. `?` Порог подлежит калибровке — если окажется слишком долго, смягчается до «8 букв с известными изолированной и конечной».

Тема форм покрывается **за один сеанс**, последовательными блоками:
конечные формы всех букв темы → начальные → срединные → повторение старого.
Перед первым заданием с незнакомой формой показывается её объяснение,
включая формы в вопросе и вариантах ответа. Будущие блоки не добавляются
в варианты заранее. Чередование букв сохраняется внутри каждого блока.
Так урок не отмечается пройденным после обучения только конечным формам.

### Формы показываются в слове

Соединённая форма — начальная, средняя, конечная — показывается не только глифом с татвилями (`ـبـ`), но и коротким **словом-примером с подсветкой этой буквы**. Глиф с хвостиками — абстракция; в слове видно, как буква на самом деле цепляется за соседей. Для несоединяющихся букв то же слово показывает разрыв. Слова задаются в контенте, по одному на форму, по возможности из уже введённых букв.

### Огласовки и слоги

Огласовка — **атом-знак**, а не свойство буквы. Их три:

```
фатха (َ ) → а
касра (ِ ) → и
дамма (ُ ) → у
```

Сочетание «буква + огласовка» — это атом типа **слог**, и такие атомы **не заводятся все 84 сразу**. Слог создаётся лениво, при первом появлении в задании, и дальше живёт как обычный атом. Так модель не раздувается, но проблемные сочетания всё равно всплывают: если человек путает `بِ`, этот конкретный слог попадёт в очередь повторений.

Аудио при этом нужно на все 84 сочетания — это требование к контенту, а не к модели прогресса.

### Модель буквы

```dart
Letter {
  id
  arabicName        // ألف
  ruName            // алиф
  sound             // фонетическое описание
  audioName         // аудио: имя буквы
  audioSound        // аудио: звук буквы
  connectsLeft      // bool — false для ا د ذ ر ز و
  forms             // [isolated, final] | [isolated, initial, medial, final]
  dots { count, position }   // 0..3, above | below
  strokePaths       // SVG-пути для обводки, по форме
}
```

---

## 3. Состояния атома

```
new → introduced → learning → known → mastered
```

| Состояние | Смысл | Переход дальше |
|---|---|---|
| `new` | ещё не встречался | урок вводит атом |
| `introduced` | показали, не спрашивали | первое задание |
| `learning` | в активной ротации | 3 верных подряд **с первой попытки** в ≥2 разных типах упражнений |
| `known` | распознаёт уверенно | подтверждение после интервала + ≥1 успех в «активной» механике |
| `mastered` | закреплено | — |

**Активная механика** = обводка или сборка слова. Одних тапов по вариантам недостаточно для `mastered`: узнавание ≠ воспроизведение.

Ошибка сбрасывает атом **на одно состояние назад**, не в ноль.

Дополнительно у атома есть два независимых флага, см. §6.4:

| Флаг | Смысл |
|---|---|
| `отложен` | временно выведен из ротации, не считается долгом |
| `weak` | засчитан по облегчённому критерию, требует добора в контексте |

Для букв и их соединённых форм календарного ожидания нет: после `known`
они возвращаются через 1 → 2 → 4 сессии и после трёх своевременных чистых
повторов переходят в `mastered`. За одну сессию засчитывается не больше
одного подтверждения, сколько бы заданий по форме в ней ни было. Для
остального материала сохраняются календарные подтверждения через
1 → 2 → 4 → 7 дней. Правила задаются по типу атома, чтобы более простой
алфавит не наследовал строгость будущего запоминания слов.

---

## 4. Типы упражнений

Одна переиспользуемая сцена с разными режимами. Обычное задание возвращает
результат по **конкретному атому**. Сборка четырёх форм — семейное задание:
она возвращает отдельный результат по каждой форме. Это единственный источник
прогресса.

| # | Режим | Ввод | Проверяет |
|---|---|---|---|
| 1 | звук → выбрать букву; варианты в той же форме, что и ответ | выбор + аудио | связь звук-графема, узнавание формы |
| 2 | показана отдельная форма буквы и четыре слота: «отдельно», «в начале», «в середине», «в конце»; все четыре формы перемешаны в плитках, человек последовательно заполняет выделенный слот, состояние меняется сразу, а поверх интерфейса летит отдельный слой: по дороге он меняет размер и вид с плитки на целевой слот, затем растворяется, а цель коротко увеличивается и возвращается к обычному размеру; по тапу на заполненный слот переход идёт обратно — от слота к плитке с таким же эффектом приземления; после четвёртой заполненной позиции весь порядок проверяется одним ответом; после ошибки слоты показывают правильность и счёт, а верные формы остаются на месте для следующей попытки; после третьей ошибки правильный порядок показывается две секунды, затем все слоты очищаются для самостоятельной сборки; режим доступен только у букв со всеми четырьмя формами | последовательный выбор | понимание форм |
| 3 | найти букву в слове | выбор | позиционное распознавание |
| 4 | режим 1 с минимальной парой в вариантах (`ب ت ث`) | выбор + аудио | точки |
| 5–7 | *(упразднены: строились на русском имени буквы)* | | |
| 8 | обвести букву по контуру | обводка | воспроизведение формы |
| 8а | написать букву по памяти | обводка без контура | воспроизведение без опоры |
| 9 | собрать слог/слово из форм | drag-and-drop | порядок RTL, соединение |
| 10 | назвать букву вслух; запись уходит на сервер, тот отвечает, какую букву услышал | голос + сервер | имя буквы, произношение |

Когда у буквы введены все четыре формы и хотя бы одна из них созрела для
интервального повтора, записи этого семейства сворачиваются в одну сборку из
плиток. Она получает один слот вместо четырёх отдельных заданий и имеет
приоритет перед письмом. Полностью верная раскладка записывает по одному
успеху каждой форме; при ошибке успех получают правильно поставленные формы,
а ошибку — только перепутанные. Это успех режима понимания позиции, а не
замена обязательной обводки или письма по памяти. Подтверждение интервала
засчитывается форме только тогда, когда её срок уже наступил. Внутри одной
сессии сборка даётся не больше одного раза на букву.

Плюс **карточка звучания** — не задание, а показ в блоке «новое»: звучит звук, одновременно показано написание. Связывает графему и звук до того, как начнутся проверки.

**Без русского имени.** Задания с выбором строятся на звуке и арабских буквах: транслит учит не читать, а сопоставлять буквы русским словам. Имя буквы показывается только как подмена — по нажатию «Не слышно? Показать название» или когда озвучки нет. Что именно звучит, решает озвучка, а не задание: сейчас это имя буквы, с огласовками и словами появится её звук. Знание соединений проверяется письмом (режимы 8 и 8а), позже — сборкой слова.

**Подписи похожих звуков.** Там, где имя буквы показывается, после русского
названия добавляется латинское обозначение в круглых скобках:
«Са (th) / Син (s) / Шин (sh) / Сод (ṣ)», «Ха (ḥ) / Хо (kh) / Хэ (h)»,
«Та (t) / То (ṭ)», «Даль (d) / Заль (dh) / Дод (ḍ)», «Зай (z) / Зо (ẓ)»,
«Кяф (k) / Коф (q)», «Айн (ʿ) / Гойн (gh)».
Обозначение одинаково во всех формах буквы и стоит перед уточнением позиции:
«Ха (ḥ) в начале». Это подсказка к различению звуков; образец произношения
даётся аудио. В связном тексте объяснений сохраняются русские имена.

### Правильность и скорость

Начальное освоение (`learning → known`) требует трёх верных ответов подряд
с первой попытки в двух разных режимах, независимо от времени. Медленное
правильное письмо или произношение не запирает пользователя в первой теме.
Исправленный ответ выполняет обязательную практику, но не растит серию;
ошибка по-прежнему её сбрасывает.

Скорость нужна для автоматизма уже знакомого материала: после `known`
только быстрые верные ответы с первой попытки увеличивают интервал повтора
и дают подтверждения для `mastered`. Медленный ответ сохраняется в журнале,
но не увеличивает эти показатели.

`?` Порог в 5 секунд подлежит калибровке; для режимов с аудио и обводкой он другой.
Письмо занимает десятки секунд, и торопить в нём нечего: порог там 40 секунд
и по сути отсекает только брошенное задание.

### Порядок письма

Режимы 8 и 8а — один холст, разница только в том, виден ли контур. Порядок
всегда один: **сначала по контуру, потом по памяти**. Просить написать по
памяти букву, которую человек ни разу не вёл рукой, — это проверка до обучения.

В шапке холста рядом с «Стереть» находится кнопка воспроизведения имени
буквы. Как в остальных карточках, первое нажатие включает запись с начала,
повторное останавливает её; во время звучания показывается значок остановки.
В обоих режимах письма звук автоматически включается при появлении буквы,
a ручная кнопка позволяет прослушать её ещё раз. Внизу карточки показывается
та же анимированная волна, что и в остальных заданиях. Без записи кнопки и
волны нет.

Для каждой базовой (отдельной) формы, изучаемой в теме урока, обязательны
**обводка по контуру, письмо без контура и произношение**, пока голосовые
задания включены, — также при повторном открытии темы. При пяти заданиях
порядок такой: контур → узнавание → письмо
по памяти → узнавание → голос. При четырёх убирается второе узнавание,
при трёх остаются три обязательных задания. На такую букву заранее выделяются
минимум три места. Автоплан выбирает посильное число букв до объяснений;
генератор не удаляет материал из готового плана. В уроках курса обязательна
вся тема: если она не помещается, её делят на отдельные уроки в программе.
Все 28 базовых форм имеют SVG для письма.

Если отдельная буква новая, её произношение переносится сразу после карточки
знакомства: объяснение буквы → произношение → объяснение следующей буквы.
После знакомства со всеми буквами идут остальные задания в прежнем порядке.
Перенос не добавляет заданий и не убирает оба вида письма. При повторном
изучении уже введённой буквы голос остаётся после письма.

Для остальных форм и букв из общего блока повтора сохраняется прежний порядок.
Письмо идёт через задание: первый слот атома — по контуру, если букву ещё
не вели рукой, дальше по памяти через одно, между ними узнавание. Так письмо
и карточки делят слоты поровну; одно письмо на урок оставляло букву карточкам,
а сплошное письмо было бы рисованием, а не чтением. Между письмом и письмом
остаётся узнавание. Буква из повторения просится
по памяти, только когда она уже в `known`: на середине пути повторению
полезнее узнавание.

Ошибка в письме показывает контур — это и есть разбор верного ответа.
Атом без осевого SVG (все соединённые формы сейчас) обводкой не спрашивается.

### Назвать букву вслух (режим 10)

Произношение имени буквы проверяет сервер: запись уходит на `POST /letter?expected=ت`,
в ответ приходит, какую букву он услышал и совпало ли (контракт в §12).
Это полноценное задание, а не отдельная дорожка: результат пишется в лог
и двигает атом, как остальные режимы. Механика **активная** — имя буквы
вспоминается, а не выбирается, поэтому она, как и письмо, открывает путь
к `mastered`.

#### Поток экрана

```
1.  Крупная буква:  ت
    «Назовите эту букву вслух» · кнопка микрофона
    Кнопки «послушать» НЕТ — озвучка была бы ответом

2.  Кнопка удерживается, пока человек говорит; отпустил — запись ушла

3.  Совпало       → верно, следующее задание
    Не совпало    → «Услышано: ط — то (толстая)», ещё одна попытка
    Второй промах → ошибка, разбор, кнопка «Ясно», задание вернётся
    Запись плохая (сервер вернул предупреждение «шумно», «тихо»)
                  → попытка не тратится, просят повторить
```

#### Когда даётся

- Только **отдельная форма**: имя у буквы одно на все формы
- Для **новой отдельной буквы — сразу после её карточки знакомства**, до объяснения следующей буквы. После ответа или пропуска продолжаются объяснения; ошибка сначала разбирается и исправляется в этом же задании
- **Ровно один раз для каждой базовой буквы темы**: для новой — сразу после знакомства, для уже введённой — после обоих режимов письма. Голос не заменяет обязательное письмо
- Для букв из общего блока повтора — **ровно один раз на каждую отдельную букву за урок**. Здесь голос выбирается раньше письма в подходящем активном слоте: иначе единственная встреча освоенной буквы всегда уходила бы письму по памяти
- Тайм-аут проверки аудио — 15 секунд после отправки записи. Если сервер не
  ответил за это время, показывается ошибка соединения, а попытка не тратится
- Порог скорости чистого ответа — 20 секунд; он влияет на дальнейшее
  закрепление после `known`, не блокирует начальное освоение и не заменяет
  тайм-аут запроса

#### Без сервера

Нет сети или сервер не отвечает — задание не проваливается и в лог не пишется:
показывается причина и кнопка «Продолжить без произношения». То же правило
действует при запрете микрофона, сбое записи и другой технической ошибке.
Можно повторить попытку; если человек продолжает без произношения, остальные
голосовые задания этой сессии удаляются и заменяются доступными режимами.
Запрет микрофона и явный выбор «Больше не предлагать произношение» сразу
отключают голос как критерий завершения темы. Сбой записи или сервера делает
это после пропуска в двух разных сессиях; повторы внутри одной сессии считаются
одним случаем. Распознанный, но неверный ответ остаётся учебной ошибкой.
Отключение не создаёт успешной записи в журнале: путь пересчитывается с правилом,
что `sayName` больше не обязателен. Голос можно снова включить в меню отладки.
Наказывать за чужую поломку нельзя.

Отладочная кнопка «Ответить верно» решает другую задачу: она имитирует настоящий
правильный ответ в текущем режиме и записывает его в журнал обычным путём.

#### Технические требования

- Запись **AAC в m4a, 16 kHz mono** на обеих платформах: сервер принимает и wav, и m4a, а wav на Android системный рекордер не умеет. Файл временный, удаляется после ответа
- Честный `NSMicrophoneUsageDescription`: микрофон нужен для проверки произношения — так и написано

#### Юридические требования

Голос — персональные данные, в трактовке GDPR близко к биометрии. Это условие прохождения ревью в обоих сторах, а не формальность.

- **Явное согласие до первой записи**, отдельным экраном — не строчкой в общей политике. `?` Экрана пока нет, см. чеклист
- Если в аудиторию попадают дети, требования жёстче (COPPA) и меняется возрастной рейтинг

`?` Сбор записей в датасет для дообучения модели (контракт `/recordings` в §12) — отдельное решение: записи этого режима туда пока не уходят.

### Дистракторы

Неверные варианты решают **две разные задачи** в зависимости от того, сколько атомов в задании.

#### Задания на один атом (режимы 1–8): дистрактор задаёт сложность

Диагностика тут не нужна — задание и так про один атом. Дистракторы определяют, что именно тренируется, и должны усложняться по мере освоения:

| Состояние атома | Дистракторы | Что тренируем |
|---|---|---|
| только введён | далёкие по форме (`ب` против `س`, `م`) | образ буквы целиком |
| `learning` | смешанные | узнавание среди похожего |
| последнее подтверждение перед `known` | минимальная пара (`ب ت ث`) | различение по точкам |

Минимальная пара — это **экзамен, а не стартовый режим**. Показывать её сразу на новой букве означало бы провалить человека за то, что он ещё не дошёл, и запустить откладывание (§6.4) там, где всё в порядке.

Побочный эффект градации полезен: `known` начинает означать «различает», а не «узнаёт при отсутствии конкурентов».

#### Составные задания (режим 9, слоги и слова): дистрактор даёт диагностику

Здесь атомов несколько, и при ошибке нужно понять, какой из них подвёл. Неверные варианты строятся так, чтобы отличаться от верного **ровно одной буквой**. Тогда выбор пользователя сам указывает на проблемный атом — отдельный микротест после провала не нужен.

#### Жёсткие ограничения на любой дистрактор

- **Только из введённых атомов.** `ث` не может быть вариантом ответа, пока `ث` не введена: иначе человек выбирает между знакомым и незнакомым, что не проверяет ничего, а просто отсеивает по узнаваемости.
- **Минимальная пара — только если обе буквы введены.** Частный случай предыдущего правила, но критичный: пара `ب`/`ث` бессмысленна, пока `ث` неизвестна.

#### Частный случай: урок про точки

На старте известны только `ا ب ت ث`, и пул дистракторов — это и есть минимальные пары. Это не конфликт с градацией, а свойство этапа: в уроке про точки минимальная пара **и есть предмет обучения**, а не подвох. Человек только что прочитал объяснение — логично сразу на нём и проверить. Градация начинает работать позже, когда пул введённых букв даёт из чего выбирать.

---

## 5. Сессия

- **До 20 заданий**, виден единый прогресс-бар с фактической длиной занятия. Обычно блоки закрепления, новое и повторение заполняют весь бюджет. Узкий урок из двух отдельных букв может закончиться раньше, если созревшего старого материала не хватает.
- Ошибка → показать верный ответ + короткое объяснение → вернуть задание через несколько шагов. Если такая же проверка этой буквы уже есть впереди, отдельная копия не добавляется; поэтому в узком двухбуквенном уроке одинаковый тест встречается не больше двух раз на букву. В полной сессии повтор заменяет добавочное задание, но не вытесняет обязательную форму или режим и не превышает бюджет.
- В обводке промах — штрих, который не продвинул текущую часть буквы: он стирается с холста сразу, а штрих, продвинувший часть из нескольких линий, остаётся. После **трёх промахов подряд по одной части** холст сам показывает, как пишется: открывает контур и показ, и по памяти, и по контуру. Это подсказка, а не ошибка: человек обводит по ней, и собранная буква засчитывается верным ответом. Ошибкой обводка становится только по кнопке «Не помню». Допуск проверки при этом не смягчается: подсказка честнее сниженной планки, а человек, у которого не выходит линия, не застревает на букве без помощи. `?` Число 3 подлежит калибровке.
- Ответ со второй попытки не даёт продвижения по состоянию атома, но и не считается повторной ошибкой.

Жизней / сердечек нет.

---

## 6. Уроки: граф вместо расписания

Фиксированной программы «урок 7 вводит `ج` и `ح`» **нет**. Такое расписание убивает адаптивность: буква вводилась бы независимо от того, готов человек или нет.

Хранятся **ограничения и заготовки**, а не последовательность. Порядок уроков выводит алгоритм.

Учебный контент — граф знаний, темы, шаблоны уроков, тексты объяснений, данные по буквам — лежит **JSON-файлами в ассетах приложения**. Версионируется через git, работает офлайн, правка требует релиза. Формат сразу проектируется так, чтобы те же файлы можно было подтягивать с сервера, когда контент начнёт меняться чаще релизов.

### 6.1 Граф знаний

Узлы — атомы и понятия, рёбра — «что после чего *можно* вводить».

```
понятие «формы»    requires: —
понятие «точки»    requires: [ا, понятие «формы»]
ب                  requires: [понятие «точки»]
ث                  requires: [ب, ت]
понятие «соединение» requires: [≥2 буквы в known]
понятие «огласовки»  requires: [≥8 букв в known]
```

Это не порядок уроков, а рёбра. Условия бывают двух видов: конкретные зависимости (`ث` после `ب` и `ت`) и пороговые (`≥8 букв в known`).

Пороговые условия считаются по **`known`, а не `mastered`**. `mastered` требует отложенных подтверждений: для букв — через 1/2/4 сессии, для остального материала — по календарю из §3. Привязка тем к нему задерживала бы курс даже тогда, когда человек всё уже узнаёт. `known` — это «узнаёт уверенно», и этого для открытия следующего понятия достаточно.

### 6.2 Шаблоны уроков

Не «урок 7», а «учебный блок такого типа». Шаблон задаёт структуру блока и правила подбора, но **не конкретные буквы**. Одна сессия может последовательно включать несколько таких блоков.

| Шаблон | Когда применяется | Состав |
|---|---|---|
| `введение понятия` | открылось новое понятие | длинный intro + закрепление понятия + повтор |
| `введение буквы` | нагрузка в норме, в графе есть доступный атом | intro + 1–2 новых атома + закрепление + повтор |
| `повторение` | нагрузка выше порога | только повтор, новых атомов нет |
| `соединение` | понятие «соединение» открыто | слоги из освоенных букв |

Блок **повтора берётся из общей очереди**, а не из атомов самой темы: иначе буквы прошлых уроков не возвращались бы никогда, и интервальное повторение существовало бы только на бумаге. Из двадцати мест не меньше четырёх резервируются возврату, остальные — теме урока. Слоты под возврат резервируются первыми, иначе тема съедает весь урок. Один атом занимает не больше пяти заданий, иначе урок вырождается в одну букву. Созревшие формы одной четырёхформенной буквы считаются одним семейным повтором и занимают один слот сборки.

Для полного блока ровно из двух отдельных букв действует более строгий предел: по четыре задания на букву — обводка, узнавание, письмо по памяти и произношение. Если голос технически недоступен, его место может занять второе узнавание, но третьего одинакового теста не будет. Все свободные места получают только буквы из интервальной очереди прошлых тем. Если такого повтора мало, занятие заканчивается раньше; следующая пара новых букв ради числа 20 в эту же сессию не вводится. Для остальных типов блоков планировщик по-прежнему может добавить следующий готовый цельный блок.

Очередь — интервальная. Атом, не дошедший до `known`, просится в повтор
каждый урок. Буквы и их формы после `known` возвращаются через 1, затем 2
и 4 сессии; каждый своевременный чистый повтор в новой сессии даёт одно
подтверждение, третье переводит форму в `mastered`. Несколько ответов в
одной сессии не увеличивают интервал. Ошибка откатывает форму в `learning`
и обнуляет подтверждения. Для остальных типов материала очередь сохраняет
общую схему 2, 4, 8, 16 сессий, а переход в `mastered` дополнительно требует
календарных интервалов из §3. Внутри очереди сначала недоученные, затем те,
у кого срок повторения вышел раньше.

Общий скелет сессии; части `пробел` и `новое` могут повторяться:
```
[ пробел ]     обязательный короткий добор недоученного
[ intro ]      объяснение нового цельного материала
[ новое ]      обязательные упражнения по нему
[ повтор ]     очередь повторений и знакомый материал до лимита
[итог]         что выучено, что открылось
```

Порядок блоков строгий: сначала материал урока и закрепление по нему, потом повтор старого отдельным блоком. Чередование атомов действует только внутри закрепления; повтор в него не подмешивается.

Каждый урок форм включает все заявленные формы букв темы в одном сеансе,
блоками по позиции (см. §2). Хотя бы одно задание выделяется каждой форме;
оставшиеся места идут на закрепление. Если у букв есть только конечные формы
(`د ذ ر ز`), достаточно одного блока. Ошибки возвращаются на повтор по §5.

Это правило действует при знакомстве с новым блоком и явном выборе
полного повтора темы в оглавлении. Автоматическое закрепление уже
показанного материала работает только с пробелами (см. §6.3).
Перед заданиями каждая форма темы получает объяснение. Лимит заданий,
накопленный прогресс и зависимости не могут исключать материал из темы.
До объяснений проверяется, что весь материал доступен и помещается вместе
с обязательным повторением старого. Если нет, нужно изменить порядок,
зависимости или разделить тему в программе курса; неполный урок не создаётся.

В `intro` для букв `ا د ذ ر ز و` обязательно показывается правило о несоединении влево.

### 6.3 Планировщик (на клиенте)

Перед началом и после каждого завершённого блока планировщик заново считает прогресс и выбирает следующую посильную цель:

1. Если одновременно отложено больше четырёх элементов — закрепление
   с досрочным возвратом самого старого отложенного.
2. Если есть начатый доступный блок с неосвоенным материалом — продолжаем
   его: ещё не показанное объясняем, уже показанное закрепляем коротко.
   Если весь блок уже показан, выбираем только неосвоенные элементы,
   начиная с тех, которые дольше не встречались. При нескольких
   таких блоках выбираем первый по программе в выбранном разделе.
   Этот приоритет сильнее нового понятия и гарантии темпа.
3. Если нагрузка выше порога — закрепление. После двух занятий без нового
   материала можно ввести доступный блок, но зависимости всё равно обязательны.
4. Иначе — первый незавершённый блок нового материала по порядку в
   оглавлении выбранного раздела. Техническая готовность более поздней темы
   не позволяет ей обогнать программу курса. Следующий этап начинается
   только после освоения или успешной входной проверки всех тем предыдущего:
   соединение нескольких букв не показывается до завершения этапа алфавита.
5. Если нового доступного блока нет — повторение из общей очереди. Когда срок
   повторения ещё не подошёл, ручной запуск занятия всё равно даёт практику
   знакомого материала, а не пустой экран.

Выход из занятия, просмотр объяснения и отметка о завершении не означают
готовность к следующему материалу. Готовность блока: понятия объяснены,
все проверяемые элементы достигли `known`; для базовых букв также выполнены
оба режима письма, если доступен контур, и произношение, пока оно включено.
Успешные режимы
считаются по всей истории верных ответов, включая медленные и исправленные;
достаточная уверенность проверяется отдельно состоянием `known`.
Зачёт `known + weak` по входной проверке или облегчённому критерию остаётся
исключением: обязательные учебные режимы не требуют повторного прохождения.

Сессия имеет верхний бюджет `tasksPerSession` (20 сейчас; число может быть откалибровано позже). Короткое закрепление — первый блок этой сессии, а не отдельное занятие. Если после него открылся новый цельный блок и все его обязательные задания помещаются в остаток, он вводится сразу. Иначе остаток заполняется повторением. Исключение — полный блок из двух отдельных букв: после него добавляется только созревший повтор старого материала, а при его нехватке сессия завершается с фактическим меньшим числом заданий.

Внутри короткого добора уже освоенные элементы не мешаются с пробелами. Если элемент уже в `known`, но пропущен обязательный режим, даётся только этот режим. Иначе нужно 2–3 встречи с учётом серии верных ответов и недостающих режимов. Ошибка возвращает задание, но не раздувает общий бюджет: повтор вытесняет добавочное, но не обязательное задание.

Фиксированная очередь незавершённого занятия не сохраняется. При каждом
возвращении материал и упражнения собираются по журналу ответов; число
заходов не открывает новые темы. Новых таблиц для этого нет. После серии
ошибок временно отложенный элемент не возвращается досрочно, кроме случая
из пункта 1: содержащий его блок ждёт окончания паузы, доступное повторение
продолжается по остальным элементам.

Мелкие авторские блоки в JSON задают цельный объём материала: группу базовых
букв или все соединённые формы этой группы. Темы служат оглавлением;
фиксированного числа занятий нет. Планировщик добавляет столько закреплений,
сколько требуется по знаниям, и после освоения добавляет новое.
Каждый следующий блок букв/форм требует освоенности предыдущего материала.
В новом блоке сохраняются все обязательные объяснения и упражнения из §6.2.
Проверка вместимости происходит до запуска; слишком большой блок нужно
разделить в контенте, а не исключать отдельные формы.

Соединение открывается после освоения двух букв и конкретных форм,
используемых в первом сочетании. Поздние сочетания требуют своих букв;
они вынесены в отдельные небольшие блоки. Поэтому соединять знакомые буквы
можно до окончания всего алфавита. В сочетаниях доступны задания в обе
стороны: выбрать запись по названиям букв и определить буквы по записи.
Хамза и другие знаки также получают вопросы в обе стороны, чтобы достижение
`known` не было невозможно из-за единственного режима.

Для графов без авторских блоков сохраняется подбор отдельных доступных
элементов. Числа нагрузки и гарантии темпа остаются настраиваемыми в коде.

### 6.4 Защита от застревания

Ограничение нагрузки («нагрузка выше порога → новых атомов нет») само по себе опасно: одна упрямая буква может заблокировать весь курс. Три предохранителя.

**Уровень 1. Атом откладывается**
```
отложен ← 5 ошибок суммарно ИЛИ 3 неудачные встречи в разных сессиях
```
Отложенный атом **исключается из расчёта нагрузки**: он не выучен, но перестаёт считаться долгом, и ввод новых букв разблокируется. Из ротации он уходит на 2–3 сессии.

**Уровень 2. Возврат другим способом**
Долбить одно и то же упражнение бессмысленно: если человек пять раз перепутал `ب` и `ت`, шестой показ тем же способом ничего не изменит. При возврате меняется подход, а не количество:
- самый лёгкий режим: далёкие по форме варианты
- подсказка про отличительный признак («у `ث` три точки — как три»)
- переход на обводку: моторика часто пробивает то, что не пробивает узнавание

**Уровень 3. Облегчённый зачёт**
После двух откладываний атом засчитывается по сниженным требованиям:
```
2 верных подряд в самом лёгком режиме   (вместо 3 подряд в ≥2 типах)
→ known, но с флагом weak
```
Флаг `weak` — это долг, а не амнистия:
- такие атомы **чаще обычного попадают в слоги и слова** на этапах 2 и 5
- флаг снимается при первом уверенном попадании там: буква доучивается в контексте, а не в лоб
- в справочнике «Алфавит» такая буква просто открыта, без отметки «освоено» — приложение не хвалит за то, чего не было

**Гарантия темпа**
```
если 2 сессии подряд не введено ни одного нового атома
    → ввести один новый принудительно, игнорируя нагрузку
```
Страховка на случай, когда долг по повторениям всё равно душит прогресс.

**Потолок отложенных: 4 одновременно**

Предохранители выше нужны, чтобы человек не застревал. Но если они срабатывают свободно, появляется противоположная беда: с плохим стартом можно набрать десяток отложенных букв и формально не застрять, фактически не выучив ничего.

```
если |отложенных| > 4
    → гарантия темпа отключается
    → досрочно вернуть самый старый отложенный, в облегчённом режиме
    → шаблон «повторение», пока счётчик не упадёт до ≤ 4
```

Это единственное правило, которое сильнее гарантии темпа: новые буквы не вводятся, пока не разобран накопленный хвост. Досрочный возврат нужен, чтобы хвост вообще мог разбираться — иначе отложенные просто ждут своей паузы, а урок-повторение работает вхолостую.

**Потолок долга**
Очередь повторений ограничена ~25 атомами. При переполнении самый застоявшийся откладывается — очередь физически не может расти бесконечно.

`?` Все числа (5 ошибок, 2–3 сессии паузы, 25 атомов в очереди, 2 сессии без новых, потолок 4 отложенных) подлежат калибровке на реальных данных.

### 6.5 Разделение ответственности

| Контент-менеджер решает | Алгоритм решает |
|---|---|
| какие понятия существуют и в каком порядке доступны | когда именно вводится следующая буква |
| условия разблокировки (рёбра графа) | сколько повторений в уроке и каких |
| тексты объяснений, аудио, примеры | какой шаблон применить сегодня |
| пороги нагрузки | нужен ли урок-повторение |

---

## 7. Этапы курса

Курс разделён на **два трека**.

### Трек «Чтение» — шесть этапов

```
1. Буквы: имена, формы, точки          ← визуальное распознавание
2. Соединение: короткие слоги          ← «как буквы слипаются», БЕЗ чтения вслух
3. Огласовки: фатха / касра / дамма    ← появляется звук слога
4. Мадд, затем сукун и шадда           ← долгота, потом отсутствие гласной
5. Чтение целых слов + танвин          ← появляется смысл
6. Доп. правила письма                 ← хамза, «Аль», та-марбута, лигатуры
```

### Трек «Таджвид» — отдельный курс

Правила произношения при чтении Корана: назализация, скрытие, слияние, знаки остановки.

Это **не седьмой этап, а второй трек**, и на то есть причины:
- правила применяются к сочетаниям слов, а не к буквам — другая механика
- без записи голоса их не проверить — другой способ проверки
- аудитория другая: сюда приходят люди, которые уже читают

`?` Содержание трека не проработано. Отдельный вопрос — входит ли он в этот продукт или становится следующим приложением.

Этапы 1–2 работают без огласовок, поэтому слоги там **немые**: показывают только форму и соединение. Задания вида «прочитай» появляются с этапа 3.

### Порядок букв

Алфавитный (алиф-ба-та) — задаётся рёбрами графа. Следствие: понятие **«точки»** обязано открыться вторым, сразу после алифа — иначе `ب ت ث` неразличимы. Побочный плюс: пользователь получает три буквы за один принцип.

Типичное начало (не расписание, а то, что выведет планировщик у среднего пользователя):
```
понятие «буква, имя, формы»   → ا
понятие «точки»               → ب ت ث
дальше — поток букв по алфавиту, темп задаёт нагрузка
понятие «соединение»          → при 2 буквах в known
понятие «огласовки»           → при 8 буквах в known
```

### Слоги

Синтетические комбинации из уже освоенных букв. Реальные слова появляются на этапе 5.

---

## 7а. Темы этапов 1–3

Полный список понятий, которые видит пользователь на главном экране. Этапы 4–6 пока не детализированы.

**Где лежит контент.** Граф и темы этапов 1–2 собраны целиком:
`assets/curriculum/stage1.json` (28 букв, 109 узлов, 23 темы) и
`stage2.json` (соединение, 12 узлов, 2 темы). Руками их не правят —
буквы описаны одной таблицей в `tool/build_curriculum.py`, он же
раскладывает их по узлам и темам.

Темы идут парами: «Буквы ج ح خ» вводит изолированные начертания, следующая
за ней «ج ح خ в слове» — остальные три формы тех же букв. Разделение нужно,
потому что формы буквы открываются только после знакомства с её отдельным
начертанием, а урок форм охватывает все формы букв группы (см. §6.2).

Формы буквы зависят только от знакомства с её изолированным начертанием,
а не выстроены цепочкой конечная → начальная → средняя. Цепочка требовала
по уроку на звено: за один заход открывалось ровно одно новое ребро.

### Этап 1 · Буквы

| # | Тема | Открывается |
|---|---|---|
| 1 | Буква, её имя и вид | старт курса |
| 2 | Точки: чем различаются `ب ت ث` | `ا`-изолированная в `known` |
| 3 | Разные формы одной буквы | `ب`-изолированная в `known` |
| 4 | Буквы, которые не соединяются слева | введена `د` |
| 5 | Хамза — N из 5 | `ا` в `known` |
| 6 | Алфавит — N из 28 | счётчик, не разблокировка |

#### Хамза — часть алфавита, а не доп. правило

Для **чтения** достаточно узнавать пять начертаний как один звук:

```
ء   أ   إ   ؤ   ئ
```

Правила выбора носителя — на алифе, на вав, на йа или отдельно — нужны только чтобы **писать**. Приложение учит читать, поэтому эти правила в курс не входят вообще.

Все пять сразу после алифа дать нельзя: `ؤ` и `ئ` сидят на вав и йа, которых человек ещё не знает. Хамза раскрывается по мере появления носителей — граф это выражает напрямую:

```
أ, إ   requires: ا в known
ء      requires: أ в known
ؤ      requires: و в known
ئ      requires: ي в known
```

Тема открывается при вводе `أ`, дальше идёт счётчиком «2 из 5» и закрывается вместе с последним носителем.

### Этап 2 · Соединение

| # | Тема | Открывается |
|---|---|---|
| 7 | Как буквы соединяются | ≥2 буквы в `known` |
| 8 | Разрыв в слове | тема 7 + ≥1 несоединяющаяся буква в `known` |

### Этап 3 · Огласовки

| # | Тема | Открывается |
|---|---|---|
| 9 | Что такое огласовка | ≥8 букв в `known` |
| 10 | Фатха, касра, дамма — 3 из 3 | тема 9 |
| 11 | Чтение слогов | все три хараки в `known` |

### Граф этапов 1–3

```
тема 1 «Буква, имя, вид»      requires: —
  ا-изолированная             requires: тема 1

тема 2 «Точки»                requires: ا-изолированная в known
  ب-изолированная             requires: тема 2
  ت-изолированная             requires: тема 2
  ث-изолированная             requires: ب-изол и ت-изол в known

тема 3 «Разные формы»         requires: ب-изолированная в known
  X-конечная                  requires: X-изолированная в known
  X-начальная                 requires: X-конечная в known
  X-средняя                   requires: X-начальная в known

тема 4 «Не соединяются слева» requires: введена د
  для ا د ذ ر ز و цепочка форм короче: изолированная → конечная

тема 5 «Хамза»                requires: ا в known
  أ, إ                        requires: ا в known
  ء                           requires: أ в known
  ؤ                           requires: و в known
  ئ                           requires: ي в known

тема 7 «Как соединяются»      requires: ≥2 буквы в known
  атомы-слоги, немые          requires: тема 7

тема 8 «Разрыв в слове»       requires: тема 7 + ≥1 несоединяющаяся буква в known

тема 9 «Что такое огласовка»  requires: ≥8 букв в known
  фатха                       requires: тема 9
  касра                       requires: фатха в known
  дамма                       requires: касра в known

тема 11 «Чтение слогов»       requires: все три хараки в known
  атомы-слоги, озвученные     создаются лениво при первом показе

тема 12 «Долгие гласные»      requires: все три хараки в known
тема 13 «Буква без гласной»   requires: тема 12
```

Порядок букв внутри этапа 1 — алфавитный, задаётся рёбрами вида `ج-изолированная requires: ث-изолированная в known`.

### Этап 4 · Знаки, меняющие чтение

| # | Тема | Открывается |
|---|---|---|
| 12 | Долгие гласные (мадд) | все три хараки в `known` |
| 13 | Буква без гласной (сукун, шадда) | тема 12 |

Мадд идёт **первым**: это всё ещё «согласная + гласная», только долгая — прямое продолжение огласовок, и проверяется тем же способом. Сукун ломает саму схему слога (гласной нет вообще) и потому концептуально тяжелее.

Шадда объединена с сукуном в одну тему не случайно: шадда — это и есть буква без гласной плюс та же буква с гласной. Одно объясняет другое.

**Танвин на этап 4 не попадает.** Он не про долготу и не про отсутствие гласной, а про окончание «-ан / -ин / -ун», и встречается только в конце настоящих слов. Его место — этап 5, см. ниже.

#### Как проверяется мадд

Мадд — единственное место, где разница чисто на слух. Проверка идёт **от звука к написанию**:

```
звучит  «ба»  →  выбрать из [ بَ · بَا ]
звучит  «баа» →  выбрать из [ بَ · بَا ]
```

Микрофон не нужен, а тренируется ровно то, что надо — слышать долготу. Для сравнения, огласовки проверяются так же, но выбором из `بَ / بِ / بُ`.

#### Атомы этапа 4

Как и с огласовками: **знак — атом**, сочетания создаются лениво.

```
мадд-алиф (ـَا), мадд-вав (ـُو), мадд-йа (ـِي)   — 3 атома-знака
сукун                                            — 1 атом-знак
шадда                                            — 1 атом-знак
```

Конкретные сочетания «буква + знак» становятся атомами типа «слог» только когда реально встретились в задании. Иначе комбинаторика взрывается: 28 букв × 3 хараки × 5 знаков — это тысячи сущностей, и курс превратился бы в бесконечный.

### Этап 5 · Чтение слов

Первый этап, где появляется смысл: настоящие слова с переводом вместо синтетических слогов. Сюда же переезжает **танвин** — как тема «Окончания слов», потому что встретить его можно только в осмысленном тексте.

`?` Остальное по этапу 5 не проработано: откуда список слов, по какому принципу отбор, когда показывать перевод, собирается слово по слогам или узнаётся целиком.

### Этап 6 · Дополнительные правила письма

Правила **письма, а не произношения**. Без них настоящий текст не прочитать, поэтому они закрывают трек чтения.

| # | Тема | Что это |
|---|---|---|
| 15 | Артикль «Аль» (`الـ`) | солнечные и лунные буквы меняют чтение |
| 16 | Та-марбута (`ة`) | читается по-разному в паузе и в потоке |
| 17 | Лигатуры (`لا` и прочие) | сочетания, пишущиеся слитно |

**Хамза сюда не входит** — она часть алфавита и живёт на этапе 1, см. выше.

Та-марбута остаётся здесь осознанно: её чтение зависит от того, пауза после неё или поток, а это понятно только когда человек уже читает слова.

Знаки остановки из того же списка правил сюда **не входят** — они относятся к паузам при чтении, то есть к треку «Таджвид».

---

## 8. Экраны

### Главный

На главном — **одно ближайшее занятие**, его цель, краткое содержание и
кнопка «Начать занятие». Темы не занимают главный экран. План составляется
по прогрессу при открытии экрана и пересчитывается после возвращения из
занятия. Кнопка передаёт в урок именно показанный план.

Витрина занятия отличается от карточек объяснений: крупные буквы из плана,
короткий заголовок и две строки о практике. Над ней — текущая неделя:
отмечены местные календарные дни, когда пользователь работал с материалом,
включая незаконченные занятия. Простой вход на экран отметку не создаёт.
Под витриной — две компактные карточки: «Мой путь» со счётчиком освоенных
букв и «Следом» с прогнозом после закрепления текущего материала. Прогноз
использует планировщик и не меняет знания. Нажатие открывает содержание
темы и реальные условия доступа; это не обещание открытия за один урок.
Когда весь материал освоен, вместо следующей темы предлагается практика.
При первом входе приветствие приглашает начать с первых букв; статистика
показывает только реальные данные.

Ниже — «Мой путь»: отдельный экран с крупными разделами («Буквы и их формы»,
«Соединение букв»). Внутри раздела раскрываются небольшие авторские блоки.
У блока нет фиксированного числа занятий. В разделе букв показывается число
освоенных букв из 28: все формы буквы должны быть известны. Хамза учитывается
отдельным материалом и не увеличивает число букв до 29.

У раздела есть кнопка «Заниматься этой темой»: планировщик выбирает доступный
материал внутри него. Нажатие на отдельный блок показывает его содержание,
освоенность и объяснения. Доступный блок можно изучить или повторить целиком.
Закрытый блок тоже нажимается: объясняет, какие знания нужны, и предлагает
«Проверить знания и открыть». На экран попадают только разделы с готовым
контентом; огласовки и последующие этапы добавятся вместе с материалом.

### Урок
Одна сцена, переключающая режимы упражнений (см. §4).

### Алфавит (справочник)
Экран буквы открывается насовсем, когда все атомы буквы достигли `known`. Открытие показывается как награда в конце урока, но живёт постоянно в разделе «Алфавит».

Содержимое экрана буквы: все формы, точки, аудио имени и звука, правило соединения, примеры слов.

### Перескок через темы

Из описания закрытого блока можно запустить проверку необходимых знаний.
Проверяется **недостающая основа для этого блока**, включая зависимости
проверяемых букв и форм. Уже освоенные элементы повторно сдавать не нужно.
Для порога по числу букв сначала берутся наиболее знакомые буквы, проверяются
все их недостающие формы. Объяснения необходимых понятий показываются перед
вопросами и сохраняются только после нажатия «Понятно».

Перед началом указано точное число вопросов. Каждый элемент проверяется
дважды: узнать название по начертанию и выбрать начертание по названию.
Варианты в проверке могут быть незнакомыми — это проверка заявленных знаний,
а не учебное упражнение. Ответы не исправляются подсказкой и не дают повторной
попытки в той же проверке. Два верных ответа подтверждают конкретный элемент
как `known + weak`; один верный ничего не открывает. Это стартовое правило,
которое подлежит калибровке, а не доказательство долговременного запоминания.

Каждое подтверждение сразу сохраняется отдельным событием `KnowledgeConfirmed`.
При выходе или частичном успехе подтверждённое не теряется. Непроверенные и
ошибочные элементы не получают зачёт. Когда все условия выбранного блока
выполнены, предлагается «Начать тему»; иначе — «Закрепить пробелы». В следующей
проверке ранее подтверждённые элементы не спрашиваются повторно.

### Состояния материала в «Моём пути»

| Состояние | Смысл |
|---|---|
| Пока закрыто | необходимых знаний ещё нет; доступны описание и проверка |
| Доступно | можно начинать изучение |
| Изучаем | часть материала знакома; показывается освоенность |
| Освоено | все элементы блока достигли `known`, понятия объяснены, обязательная практика выполнена (или знания подтверждены отдельным зачётом) |

Главная кнопка не связана с галочкой блока: завершение занятия обновляет
знания, затем собирается следующий план. Текущий блок может требовать ещё
нескольких занятий. Ошибка может уменьшить освоенность, но **не закрывает
ранее доступный материал**: для доступа учитывается история достижения
`known` (`knownAt` сохраняется при откате), а для счётчика — текущее знание.
Старые отметки прохождения сохраняют доступ пользователям прежней версии,
но не подменяют счётчик освоенности. Новые занятия из главного экрана и
«Моего пути» таких отметок не создают.

### Онбординг

Три ветки вместо двух — потому что треков теперь тоже два.

```
«Насколько вы знакомы с арабским письмом?»

  ├─ Совсем не знаком
  │    → трек «Чтение», урок 1
  │
  ├─ Знаю часть букв
  │    → диагностика по алфавиту (10–15 заданий)
  │    → проставляются статусы атомов
  │    → трек «Чтение» с нужного места
  │
  └─ Умею читать
       → входной тест: чтение слов с огласовками
       ├─ сдал      → трек «Таджвид»
       └─ не сдал   → трек «Чтение» с подходящего места
```

**Входной тест обязателен, декларации недостаточно.** «Умею читать» — ненадёжная самооценка: люди, читающие по слогам с ошибками, уверены, что читают. Пустить их в таджвид по заявлению означает отправить туда, где они не справятся, и потерять.

Формулировка результата при провале — не отказ, а маршрут: «начнём вот отсюда», с указанием, что именно уже засчитано. Человек не должен почувствовать, что его завернули.

---

## 9. Геймификация

Только **стрик** (дни подряд) + напоминания.

Сознательно не делаем: жизни/сердечки, XP и уровни, лиги, соревнование. Причина — религиозный контекст, где азартные и соревновательные механики неуместны.

---

## 10. Монетизация

Бесплатный старт → подписка. **Одна подписка на оба трека**, раздельных покупок нет: выбор при оплате снижает конверсию, а «всё включено» повышает воспринимаемую ценность.

- Первые **~5 уроков** бесплатно: человек успевает выучить несколько букв и почувствовать результат.
- Дальше paywall.
- `?` Раз уроки генерируются, «5 уроков» — это счётчик сессий, и у медленного пользователя он истечёт раньше, чем тот увидит результат. Альтернатива: привязать границу к **теме** («бесплатно до понятия „соединение“»), тогда все доходят до одинаковой точки ценности. Решить до релиза.
- Раздел «Алфавит» для уже открытых букв остаётся доступным всегда.

---

## 11. Хранение прогресса

Сейчас — **локально**, с прицелом на бэк.

- Интерфейс `ProgressRepository`, локальная реализация (Isar / Hive).
- Пишется **не снимок состояния, а лог событий**:
  ```
  { atomId, exerciseType, correct, attemptIndex, timestamp }
  ```
- Состояние атома = свёртка лога.

Тогда переезд на бэк — это загрузка накопленного лога, без переписывания логики. Если хранить только снимок, историю и возможность пересчитать модель мы потеряем.

---

## 12. Контент-задачи

### Аудио (~150–200 файлов)
- 28 имён букв, 28 звуков, 28 × 3 огласовки, набор слогов
- Готового лицензируемого пакета на рынке нет: ASR-датасеты записаны для распознавания и не годятся для проигрывания, коранические аудио — уровня аятов, не букв
- **Решение: генерируем через TTS.** Формат файлов фиксируем сразу, чтобы при необходимости заменить на живую запись без изменений в коде

### SVG-пути для обводки (~100 атомов)
- Источник: **Noto Sans Arabic**
- ⚠️ Глиф шрифта — это замкнутый контур (периметр буквы), а не траектория письма. Автоматическая выгрузка даёт «обведи букву по краю», а не «напиши букву»
- **Решение:** скелетизировать контуры скриптом → вручную поправить пути и проставить порядок штрихов. Часы работы, но не автомат
- Обводка не блокирует MVP: упражнения 1–7 и 9 работают без неё
- Сделано: `ا ب ت ث س ش ط ظ` в изолированной форме (`assets/svg/alphabet`).
  Атом ссылается на файл полем `tracing`; без него режимы 8 и 8а пропускаются

### Тексты
Все объяснения и правила — на русском, с заделом под английский.

### Бэкенд для записей произношения

Делается параллельно с клиентом. Минимальный контракт, чтобы две части не разъехались:

**Проверка буквы (режим 10, работает)**
```
POST /letter?expected=ت
  multipart: audio (wav или m4a)

200 { "услышано": "ط", "подсказка": "то (толстая)", "оценка": 13,
      "ближайшие": [ {"буква": "ط", "похожесть": 0.945}, … ],
      "запись": { "речь_с": 0.92, "громкость_дБ": -11.2,
                  "чистота_дБ": 37.5, "предупреждение": null },
      "ожидалось": "ت", "совпало": false, "секунд_счёта": 0.26 }
400 { "ошибка": "нет звука в запросе" }
```
- `совпало` — вердикт; `предупреждение` («шумно», «тихо») — запись плохая, а не произношение
- Локально сервер слушает `localhost:8765`, эмулятор Android ходит на `10.0.2.2`

**Загрузка**
```
POST /recordings
  multipart: audio (opus) + meta (json)
  meta: { recordingId, anonUserId, atomId, слог, эталон,
          selfAssessment, atomState, attempt,
          device, appVersion, recordedAt }
```
- `recordingId` — **uuid, генерируется на клиенте**. Повторная отправка того же id идемпотентна: сеть ненадёжна, ретраи неизбежны
- `anonUserId` — стабильный анонимный идентификатор, **не связанный с email и аккаунтом**. Датасет не должен позволять опознать человека

**Очередь на устройстве**
- выгрузка батчами, **только по Wi-Fi**, с экспоненциальными ретраями
- локальный файл удаляется **только после ответа 200** — иначе записи теряются молча
- потолок локальной очереди: при переполнении дропаем самые старые и пишем это в аналитику, а не растём бесконечно

**Удаление**
```
DELETE /recordings?anonUserId=...
```
Право на забвение — обязательный пункт для ревью, не опциональный. Вызывается по кнопке в настройках.

`?` Сроки хранения сырых записей и политика их анонимизации — решить до первой выгрузки.

---

## 13. Открытые вопросы

- [x] Интервалы повторений: в сессиях, с удвоением (§6.2); числа подлежат калибровке
- [ ] Содержание первых 5 бесплатных уроков поштучно
- [ ] Пуш-уведомления: когда и о чём
- [ ] Экран потери стрика / заморозка стрика
- [ ] Цена и структура подписки
- [ ] Что показываем, когда все буквы освоены, а этап 3 ещё не открыт
- [ ] **Этап 5 не проработан.** Откуда список слов, принцип отбора, когда показывать перевод, собирается слово по слогам или узнаётся целиком
- [ ] **Трек «Таджвид» не проработан.** Содержание, механика проверки, входит ли он в этот продукт или становится следующим приложением
- [ ] Состав входного теста на трек таджвида: сколько слов, какой порог
- [ ] **Диагностика в онбординге.** Как из 10–15 ответов вывести состояния сотни атомов. Механизм нетривиальный; в MVP допустимо, чтобы все начинали с нуля
- [ ] Что происходит после этапа 6 — конец курса
- [ ] Порог «8 букв» до огласовок: 32 атома в `known`, ~20–25 уроков. Проверить на живых данных, не слишком ли долго
- [ ] **Экран согласия на запись голоса** перед первым заданием «назови букву» — условие ревью в сторах
- [ ] Сроки хранения сырых записей и политика анонимизации (бэкенд делается параллельно, контракт в §12)
- [ ] Момент вербовки добровольцев: после этапа 1 или после N-й сессии
- [ ] Чем мотивировать добровольцев: счётчик вклада, бонус, что-то ещё
- [ ] Порог скорости для каждого режима упражнений
